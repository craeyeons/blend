{
  "_comment": "EXTREME test set only. Train list is empty so train_router_multi.py won't run; use --skip-train and reuse existing time_multi_runs/router_runs/run_{0,1,2}/router.weights.h5 trained on the prior full config set. Test #1: r=0 (no obstacle, pure channel flow) — does the router degenerate gracefully when the residual is uniformly small? Test #2: r=0.2 at (0.5, 0.8) so the cylinder edge sits at y=1.0, touching the top wall — extreme confinement, OOD relative to training (max train r=0.15, all interior).",
  "train": [],
  "test": [
    {
      "inlet_velocity": 1.0,
      "cylinder_radius": 0.0,
      "cylinder_x": 0.5,
      "cylinder_y": 0.5,
      "pinn_path": "./models/pinn_cylinder_Re100_0.5_0.5_0.0.h5"
    },
    {
      "inlet_velocity": 1.0,
      "cylinder_radius": 0.2,
      "cylinder_x": 0.5,
      "cylinder_y": 0.8,
      "pinn_path": "./models/pinn_cylinder_Re100_0.5_0.8_0.2.h5"
    }
  ]
}
